# Self-Healing Data Pipeline — Dependencies

# LLM client (NVIDIA API uses OpenAI-compatible interface)
openai>=1.30.0

# Data processing
pandas>=2.2.0
numpy>=1.26.0

# Database connectors
psycopg2-binary>=2.9.9       # PostgreSQL
sqlalchemy>=2.0.0             # ORM / DB abstraction

# Validation (optional but recommended)
great-expectations>=0.18.0

# Configuration
pyyaml>=6.0.1
python-dotenv>=1.0.0          # Load .env for API keys

# Logging
loguru>=0.7.2

# HTTP / API ingestion
requests>=2.31.0
httpx>=0.27.0                 # Async HTTP (for streaming sources)

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Utilities
tqdm>=4.66.0                  # Progress bars for batch processing
tenacity>=8.3.0               # Retry logic with backoff